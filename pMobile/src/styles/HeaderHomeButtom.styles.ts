import styled from 'styled-components/native';
import colors from '../constants/colors';

export const HomeButtonWrapper = styled.TouchableOpacity`
  margin-left: 12px;
  padding: 8px;
  border-radius: 10px;
  background-color: transparent;
`;
